name = "whosthedumbass-api"
main = "worker.js"
compatibility_date = "2024-01-01"

# KV namespace for nonces
kv_namespaces = [
  { binding = "NONCES", id = "YOUR_KV_NAMESPACE_ID" }
]

# Environment variables (set via dashboard or wrangler secret)
# HMAC_SECRET = "your-secret-key-32-bytes-minimum"
# TURNSTILE_SECRET = "your-turnstile-secret-key"

[vars]
# Non-secret vars can go here
